import React from 'react';
import Flux from 'react-flux-dash';
//import AnyStore from '/path/to/store';

export default class ${fileName} extends Flux.View{
    constructor(){
        super();
/*
        // bind your store on the view
        this.bindStore(AnyStore, function(){
            var anyUpdatedData = AnyStore.anyGetter();
        });
*/
    }
    
}